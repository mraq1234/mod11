// Import Actions
// Initial State
const initialState = {};

const KanbanReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default KanbanReducer;
